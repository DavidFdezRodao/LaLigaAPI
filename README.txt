Para poder crear la base de datos que alimente a la API hay que ejecutar los scripts que hay en el txt de scripts.

Para que funcione la API en el web.config en la parte de <connectionStrings> hay que editar la etiqueta LaLigaEntities para que conecte con la base de datos creada.

Para poder realizar las proebas se pasará la colección de postman.